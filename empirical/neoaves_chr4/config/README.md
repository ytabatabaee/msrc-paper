Configuration files for the Neoaves chromosome-4 empirical analysis.

The current scripts keep their default parameters in code so historical runs are
fully reproducible from the script versions and manifests. Add new configurable
run profiles here rather than mixing them into `results/`.
